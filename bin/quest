#!/usr/bin/php
<?php

require __DIR__ . '/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use RiotQuest\Components\CommandLine\CreateDocumentationCommand;

$app = new Application();

$app->add(new CreateDocumentationCommand());

$app->run();
